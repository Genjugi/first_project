CREATE TABLE if not exists mytable(    id serial primary key,    name VARCHAR(255) NOT NULL,    lastname VARCHAR(255) NOT NULL,    birthday date,    biletnum VARCHAR(40) NOT NULL);CREATE TABLE if not exists studentsdata(    biletnum varchar(40) not null,  groupe varchar(255) not null,  curse integer not null,  faculty varchar(200) not null);insert into mytable (name, lastname, birthday, biletnum) values ('test','testovich',to_date('01.01.2000','dd.mm.yyyy'),'01-test-001');insert into studentsdata  values ('01-test-001','test','1','testfac');